# Brainstorming / visual companion (session artifacts)
.superpowers/

# Claude Code session config (CLAUDE.md, memory) — kept locally only
.claude/

# Internal design specifications and implementation plans — kept locally only
docs/superpowers/

# Original hand sketch — kept locally, not version-controlled
skica/

# Rust — dashboard
/dashboard/target/
**/*.rs.bk

# .NET — sensord
sensord/**/bin/
sensord/**/obj/
/sensord/**/publish/

# Build output
/build/out/
/dist/

# OS / editor
Thumbs.db
.DS_Store
.vs/
.vscode/
.idea/
*.user
*.swp

# Build-time vendored binaries (downloaded by build.ps1 on demand)
/build/vendor/

# egui_kittest snapshot scratch files (only the canonical `.png` is checked in)
**/tests/snapshots/**/*.new.png
**/tests/snapshots/**/*.old.png
**/tests/snapshots/**/*.diff.png
